name: localization_pro
description: "A dynamic Flutter package for seamless localization management."
version: 1.1.2+1
homepage:
repository: https://github.com/ahadjonovss/localization_pro

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: